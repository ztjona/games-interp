# Core dependencies
docopt==0.6.2
numpy>=1.24
PyYAML>=6.0
scikit-learn>=1.3
torch>=2.0           # GPU: install from https://pytorch.org with matching CUDA version
tqdm>=4.64

# Game library (local editable install — NOT on PyPI)
# On a new machine: pip install -e /path/to/Quartopy
# quartopy==0.1.0  # kept for reference; install manually

# Visualization / export (optional)
dash>=2.0
plotly>=5.0
onnxruntime>=1.17    # only needed for export_onnx.py parity check

# Development
pytest>=7.0
